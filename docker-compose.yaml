version: '3.9'

services:
    blog_service:
        build: .
        ports:
            - ${PORT}:3000
        restart: always
        environment:
            - PORT=${PORT}
            - MONGO_URI=${MONGO_URI}
            - MONGO_PASSWORD=${MONGO_PASSWORD}
            - JWT_SECRET=${JWT_SECRET}
